import metadata from '../../config/metadata.json';

interface Metadata {
  title: string;
  headerTitle: string;
  description: string;
  github: string;
  linkedin: string;
  author: string;
  siteUrl: string;
}

export default metadata as Metadata;
